api.studyreef.com {
	reverse_proxy reef-server:8000
}

agents.markshteyn.com {
	# WebSocket server for Instagram browser sessions
	handle /ws/* {
		reverse_proxy host.docker.internal:3001
	}

	# Portal (Next.js) — catch-all
	handle {
		reverse_proxy host.docker.internal:3000
	}
}
