# Icons

Place the following PNG files here before distributing the extension:

- `icon16.png`  — 16×16 px
- `icon48.png`  — 48×48 px
- `icon128.png` — 128×128 px

Until real icons are added, Chrome will fall back to a default puzzle-piece icon.
The extension loads and functions correctly without these files.
